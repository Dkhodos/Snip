# Global secrets — shared across environments.
# Values managed manually in GCP Secret Manager. This file declares WHAT must exist, not the values.
secrets:
  - key: cloudflare_api_token
    secret_name: snip-cloudflare-api-token
